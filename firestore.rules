rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全クライアントアクセスを拒否。
    // サーバー側は firebase-admin (Application Default Credentials) でバイパス。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
